import 'package:flutter/material.dart';

class PublicationsInfo extends StatelessWidget {
  const PublicationsInfo({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
